{
  "tesserae_compat": "1.x",
  "name": "Spotify, Queue",
  "version": "0.2.0",
  "kind": "widget",
  "description": "Currently-playing Spotify track + the next few items in your queue. Pulls from /v1/me/player/queue via the shared spotify_core OAuth. Requires Spotify Premium, the queue endpoint is gated to paid accounts.",
  "icon": "ph-queue",
  "supports": { "sizes": ["sm", "md", "lg"] },
  "cell_options": [
    {
      "name": "max_items",
      "type": "number",
      "label": "Queue length",
      "default": 6,
      "min": 1,
      "max": 12,
      "help": "How many upcoming tracks to show under the now-playing lede. Spotify usually returns up to ~20."
    },
    {
      "name": "show_now_playing",
      "type": "boolean",
      "label": "Show now-playing lede",
      "default": true,
      "help": "Lead with the current track (album art + title) before the queue list. Turn off for a queue-only feed."
    }
  ],
  "fragments": [
    { "id": "full", "label": "Full card", "w": 280, "h": 220, "icon": "ph-queue" },
    { "id": "list", "label": "Queue list", "w": 260, "h": 180, "icon": "ph-list" },
    { "id": "current", "label": "Now playing", "w": 260, "h": 80, "icon": "ph-play" }
  ],
  "render": { "needs_network": true }
}
